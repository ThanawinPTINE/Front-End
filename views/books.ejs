<!DOCTYPE html>
<html>
<head>
    <title>Books</title>
</head>
<body>
    <h1>Books</h1>
    <table>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Actions</th>
        </tr>
        <% for(val i=0; i < books.length; i++) { %>
        <tr>
            <td><%= books[i].title %></td>
            <td><%= books[i].author %></td>
            <td>
                <a href="/book/<%= books[i].id %>">View</a>
                <a href="/update/<%= books[i].id %>">View</a>
                <a href="/delete/<%= books[i].id %>">View</a>
            </td>
        </tr>
        <% } %>
    </table>
    <a href="/creat">Create New Book</a>
</body>
</html>